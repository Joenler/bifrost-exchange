// bigscreen/src/App.tsx
// Placeholder: single <main> + <h1> + <p>. The real spectator dashboard
// replaces the whole tree when the live feed lands.

export function App() {
    return (
        <main>
            <h1>Hello BIFROST</h1>
            <p>Spectator dashboard placeholder. The live feed ships in Phase 10.</p>
        </main>
    );
}

export default App;
