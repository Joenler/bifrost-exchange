# contracts/

Placeholder — Phase 01 (Contracts v1) lands the gRPC `.proto` files here:
`strategy.proto`, `auction.proto`, `round.proto`, `market.proto`, `events.proto`, `mc.proto`.
Phase 01 CI publishes generated NuGet + pip packages from this directory.

Until Phase 01 ships, this directory holds only this README and `UPSTREAM.md`.
