# gRPC ↔ RabbitMQ DTO translation map

Placeholder. This document will be authored with the Contracts v1 work: a table
mapping every gRPC type under `contracts/` to its internal RabbitMQ DTO
counterpart under `src/contracts-internal/`.

This file exists today only to reserve the path. Do not add wire-mapping
content here until the contracts v1 work lands.
